server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/localconnect?useUnicode=true&characterEncoding=utf8
    username: appuser
    password: JavaWinona2025!
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update  # dev only;
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: sep.gruppe.6@gmail.com
    password: fynfzxorwiufbkas
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
    default-encoding: UTF-8


app:
  jwt:
    geheimnis: "changeme-super-geheimes-base64-oder-lang-plain-secret"
    ablaufMillis: 86400000 # 24h
  admin:
    emails: "admin@localconnect.test, voznenko7@gmail.com"
  cors:
    erlaubteUrspruenge: "http://localhost:5173"
  debug-mail: false
  mail:
    from: "LocalConnect <sep.gruppe.6@gmail.com>"

logging:
  level:
    group.backend.mail: DEBUG
    group.backend.auth: DEBUG
    org.hibernate: warn
    org.hibernate.SQL: OFF
    # Hibernate 5:
    org.hibernate.type: OFF
    org.hibernate.type.descriptor.sql.BasicBinder: OFF
    # Hibernate 6:
    org.hibernate.orm.jdbc.bind: OFF
    org.hibernate.orm.jdbc.extract: OFF
    org.hibernate.stat: OFF
    org.springframework.jdbc.core.JdbcTemplate: OFF
    org.springframework.jdbc.core.StatementCreatorUtils: OFF